# AGENTS.md

## 项目身份

- 使用中文名芽绘台, 英文名 SproutCanvas, 包名 `sprout-canvas`.
- 保持多 Provider 图像生成工作台定位: 文生图, 参考图生成, 蒙版编辑和系列策划.
- 不添加视频生成, LoRA 训练, 团队协作, 充值支付或云端作品库. 使用访问码与灵感点管理测试额度, 视频分镜只生成静态图片.
- 按 `docs/PRD.md`, `docs/DESIGN.md` 和 `docs/FEATURE_ARCHITECTURE_REVIEW.md` 执行当前要求. 将 `docs/UI_V3_PLAN.md` 视为历史记录.
- 保留 Stitch 五张正式稿的布局和视觉语言. 以用户后续决策覆盖原稿的功能假设: 移除作品精选收藏, 回收站和不支持的模型参数.

## 常用命令

| 命令 | 要求 |
|---|---|
| `npm start` | 构建前端后启动 `server.mjs`, 默认 `127.0.0.1:8787` |
| `npm run start:local` | 构建后运行隔离的本机测试服务, 默认 8789; 独立数据与管理员凭据规则见 `docs/DEPLOY.md` |
| `npm run start:server` | 启动已有 `dist/` 的 Node 服务 |
| `npm run dev:api` | 启动 API 服务 |
| `npm run dev` | 启动 Vite, 同时运行 API 服务以供 `/api`, `/health`, `/ready` 反代 |
| `npm run build` | 执行 TypeScript strict 检查和 Vite 构建 |
| `npm test` | 执行 `node --test tests/*.test.mjs`, 再构建前端 |
| `npm run test:browser` | 使用 Playwright, 隔离服务和虚拟上游验证浏览器交互 |
| `npm run test:docker` | 先构建 `sprout-canvas:verify`, 再验证容器, 使用独立虚拟配置 |
| `npm run pm2:start` | 构建后按单实例 PM2 配置启动 |
| `docker compose up -d` | 挂载 `config/`, `logs/`, `data/`, 将容器 8787 映射到主机 8888 |

- 使用 Node.js >=22.13, 推荐 Node.js 22 LTS 最新补丁版本. 服务端依赖 Node 内置 `node:sqlite`.
- 使用 Node 内置测试执行器与 `node:assert/strict`. 单个测试直接运行 `node tests/<name>.test.mjs`.
- 将真实付费 API 验证与自动测试分离, 不让默认测试读取真实密钥或调用上游.
- 将测试产物放临时目录; 使用 `SPROUT_TEST_OUTPUT` 指定截图/报告目录.

## 模块边界

| 模块 | 保持的职责 |
|---|---|
| `server.mjs` | 同源 HTTP 路由, 静态文件, 健康检查, 生命周期 |
| `server/config.mjs` | 集中加载/校验配置, 输出不含密钥的公开配置 |
| `server/auth.mjs`, `server/http.mjs` | 会话, Cookie, 请求边界, 错误脱敏 |
| `server/text.mjs`, `server/text-routing.mjs` | 提示词/分镜文本调用及文本通道熔断 |
| `server/queue.mjs` | 单 worker, 用户内 FIFO, 用户间公平轮询, 幂等, 置顶, 领取和恢复 |
| `server/provider-adapter.mjs` | Images / Responses 适配, multipart, 完成事件解析 |
| `server/upstream-outcome.mjs` | 图片/文字统一上游结果分类, 区分明确拒绝, 未连接成功与结果未知 |
| `server/generation-input.mjs`, `server/image-result.mjs` | 输入图/蒙版校验, 图片结果读取和真实 MIME/尺寸识别 |
| `server/state-store.mjs` | SQLite 任务元信息和哈希会话 token |
| `server/identity-store.mjs`, `server/credits-store.mjs`, `server/credit-routes.mjs` | 访问码与浏览器身份隔离, 预占/结算/流水, 后台额度 API |
| `server/style-store.mjs`, `server/style-routes.mjs` | 风格 SQLite, 示例图文件, 公开读取, 管理增删改与备份事务 |
| `server/admin-auth.mjs` | 独立管理会话, 密码轮换失效, 同源校验与登录限速 |
| `server/admin-overview.mjs` | 管理员专用只读概览, 汇总风格与结算记录, 不改变任务或额度 |
| `shared/style-contract.mjs` + `.d.mts` | 风格字段, 图片/容量限制和前后端共享类型 |
| `shared/generation-contract.mjs` + `.d.mts` | 前后端统一请求类型与运行时校验 |
| `shared/series-planning.mjs` | 按输入内容策划的通用拆解规则, 结果校验和独立分镜提示词 |
| `shared/prompt-polish.mjs`, `src/lib/prompt-history.ts` | 润色指令与可用结果校验, 当前草稿最近一次润色的前后版本 |
| `src/app/App.tsx` | 四页导航, 独立管理入口, 登录, 队列和作品动作集成 |
| `src/pages/StyleAdmin.tsx`, `src/hooks/useStyleCatalog.ts` | 风格管理表单, 图片上传/拖拽/粘贴, 动态目录同步 |
| `src/pages/AdminPage.tsx`, `src/pages/AdminDashboard.tsx`, `src/pages/AccessCodeAdmin.tsx`, `src/lib/credits.ts` | 独立管理外壳, 仪表盘与管理操作入口, 访问码与点值管理, 用户端余额同步 |
| `src/hooks/useSeriesStudio.ts` | 系列身份, 草稿, 单镜版本和续交业务 |
| `src/components/image/ReferenceImageSlots.tsx`, `src/components/image/ReferenceImagePreview.tsx` | 单图和系列共用四槽位参考图列表, 逐张操作与原图预览 |
| `src/hooks/useQueue.ts`, `src/hooks/useQueueDelivery.ts` | 提交与元信息轮询, 独立结果领取, 本地事务后的确认及领取重试 |
| `src/lib/queue-recovery.ts` | 旧版或换码后的任务确认, 新请求身份与系列参考链恢复 |
| `src/lib/storage/gallery-db.ts` | IndexedDB v4 元信息, 原图 Blob, 缩略图, 领取记录, outbox 和草稿 |

## 生成与存储约定

1. 将图像单任务提交到 `POST /api/jobs`, 多图和系列提交到 `/api/jobs/batch`, 使用稳定 `requestId`, `clientContext` 和 `creditQuote`.
2. 先将配方及参考素材保存在浏览器 outbox, 再发请求. 同一请求编号不得代表不同意图.
3. 由服务端校验能力并选择 Provider, 转换 Images JSON / multipart 或 Responses 图片工具调用.
4. 通过 `/api/jobs/me` 轮询, 活动任务和未领取结果完整返回, 已结束历史使用游标分页.
5. 将所有返回图片和已消费 job ID 放在同一个 IndexedDB 事务中. 事务成功后才发送 `/api/jobs/:id/ack`.
6. 使用稳定完成时间. 删除作品后不得再次导入已消费任务. 删除系列时处理其所有版本.
7. 将生成作品/参考图/蒙版保存在浏览器. 服务端仅对生成图片进行内存短期中转, 通常 30 分钟, 领取且无活动依赖后释放. 允许管理员录入的风格示例图持久化到风格数据目录, 不将其作为用户作品库.
8. 将任务元信息保留 7 天, 将点数操作幂等记录和账本独立持久保留. 重启后 pending 释放预占且可恢复, running 保留预占待核实, 未领取成功结果标记为已过期且不退点.
9. 从本地配方恢复未执行任务. 对结果未知的付费请求要求用户明确重新生成, 禁止自动重发.
10. 让后续分镜使用首镜参考. 首镜内存已释放时使用本地原图快照, 服务端校验作品身份和图片 SHA256.

## 必须守住的约束

- 只在用户明确要求并确认后执行 Git commit, 不自行提交, 不 amend.
- 永远不将 API Key 下发到浏览器. 所有上游调用必须经服务端模块并使用 `readLocalConfig()`.
- 仅用访问码授权普通工作台, 不恢复共享密码或免密码生成. 访问码无有效期, 多人共用余额, 从服务端 Cookie 会话确定任务归属, 不信任客户端 UUID.
- 将整批入队与灵感点预占放在同一 SQLite 事务. 成功扣除, 明确失败/执行前取消返还, 结果未知保留预占供管理员核实, 不自动重发上游.
- 不按 HTTP 成功状态直接扣点, 校验完整可用结果并识别正文或流式错误. 将网关超时和截断响应保留为未知; 仅在确认未执行或明确失败时安全切换通道.
- 不因浏览器刷新/关闭取消服务端任务或返还已成功点数. 优雅关闭时等待在途图片/文字和待落账结算; 结果过期和历史清理的存储失败只重试写库.
- 区分客户端提交中与提交待确认, 不将上传或待确认映射为生成失败. 将原图下载/本地保存/领取确认与元信息轮询独立执行, 同页面最多领取 2 项且单任务去重, 不改变服务端单 worker. 对领取错误只重试原结果, 已消费任务只补确认; 迟到受理响应不得重建已清理的 outbox, 退出或换码后不得以新身份确认旧领取.
- 为图片和文字保留稳定请求编号与点值快照, 旧报价需再次确认. 换码不得静默移转未完成请求, 数据库写失败仅重试落账.
- 对系列拆解在服务端校验 JSON, 幕数与每幕必填内容后再扣文字点数. 复用共享分镜解析规则; 刷新后的待领取文字先按原请求编号读取, 不重新调用模型.
- 让润色按原文完整程度调整. 短句补足可画的场景, 构图, 光线与动作瞬间, 禁止只改书面语; 已完整的提示词只整理表达. 保留语言, 主体, 数量, 品牌, 地名, 引用文字, 占位符与限制, 不强行压缩到一句话或 60 字. 校验非空与生成长度上限后才结算, 不截断结果后收费. 将结果和最近一次前后版本保存到当前草稿后再结束领取, 保存失败或指令更新时复用原请求; 迟到结果不得覆盖已切换或其他标签页更新的草稿. 允许免费撤销/恢复并保留两侧手工修改, 不因撤销退还成功调用点数.
- 只在创建/重置时返回完整访问码, 仅存哈希与尾号. 默认创建有限额度码, 允许管理员显式选择无限额度, 不自动赠送无限余额, 不添加 IP 历史或设备指纹.
- 为每个任务保存额度模式快照, 切换无限/有限时保留余额, 不改变已受理任务的扣点规则. 无限任务不预占或扣减有限余额, 仅记录等值用量; 新请求的额度报价过期时要求重新确认.
- 删除访问码时撤销授权, 隐藏列表记录并取消未执行任务, 保留审计流水和原浏览器结果领取. 存在运行中或待核实操作时拒绝删除, 不提供恢复入口.
- 对访问码删除, 重置和待核实结算只提供一次确认, 不展示原因输入, 由服务端自动记录操作. 保留版本校验, 仅在发生版本冲突时展示刷新数据; 刷新后保留已编辑字段并同步未编辑字段, 经用户再次确认才保存或执行. 网络失败重试沿用原请求编号, 不以刷新方式重复执行点数操作.
- 点击访问码卡片打开设置弹窗, 用卡片上的用量图标打开独立流水/核实弹窗. 将无限额度开关放在点数输入框右侧, 开启后禁用输入且保留原有限余额. 对有限额度的图片/文字操作复用浅色圆角点数标记, 按真实数量和点值显示消耗; 无限额度隐藏按钮内标记, 仅在顶栏展示无限灵感点. 旧任务按自身额度模式显示, 不被当前模式覆盖.
- 将访问码备注作为用户可见的访问名称, 从当前会话只读返回; 后台明确标注用户可见, 空名称显示创作者. 在前台顶栏加粗灵感点, 保留任务队列和用户浮窗; 将深浅色, 使用帮助与退出收进浮窗, 不展示通道健康状态.
- 将单图画卷限制为最多 4 个创作记录, 优先当前批次, 再保留其他未完成任务, 不足时以近期作品补位, 同一任务的排队/生成/保存/结果原位更新. 保留完整本地展馆和队列记录; 两栏随整页滚动, 不使用控制栏吸顶或双区独立滚动.
- 为四页共用顶栏保留 80% 纸面底色和 24px 背景模糊. 提示词使用单层外部焦点轮廓, 润色期间改为植物绿边框流光并只读, 尊重减少动态效果设置.
- 停用/重置时取消未执行任务并释放预占, 保留已开始任务的结算与原浏览器结果领取. 不因重放旧管理操作取消新授权任务.
- 使用一个 Node 进程和一个图像 worker. 固定 `imageConcurrency: 1`, 不增加 PM2 cluster worker 或容器副本.
- 保持用户间轮流调度, 用户队列清空后继续原轮换位置. 仅在我的任务抽屉展示全站生成中/等待总数, 单图与系列保留本人任务状态, 顺序和耗时, 不重复展示全站统计, 不将个人位次当成全站名次. 排队/渲染秒数使用服务端时间基准和浏览器单调时钟每秒更新, 保留活动任务每 2 秒轮询; 断连标记上次状态, 不伪造预计完成时间.
- 无实时进度时不显示固定比例进度条. 用静态时钟表示排队或等待领取, 用活动图标表示实际处理并配合真实耗时; 失败停止动画并保留重试入口, 尊重减少动态效果设置.
- 不把 `config/local.config.json`, `data/`, `logs/` 或测试生成图片提交到 Git.
- 修改配置字段时同步 `config/local.config.example.json` 与 `docs/DEPLOY.md` 字段表.
- 将图像环境变量覆盖到所选默认图像通道, 将 `TEXT_*` 覆盖到首个文本通道, 保留其他通道独立配置.
- 拒绝生产占位密码/密钥, 不绕过校验. 配置与部署细节只在 `docs/DEPLOY.md` 维护.
- 保存真实输出格式和尺寸, 不根据请求值伪造文件类型. 不提供 Seed/CFG/步数/参考权重占位控件.
- 从作品进入局部重绘时继承原配方, 固定画幅/质量/背景/格式/压缩率, 使用原通道与原图文件, 不叠加其他画风或隐式缩图. 将参考图生成与局部重绘明确区分, 清空蒙版不得变成整图生成.
- 在单图参考模式支持粘贴和拖拽追加图片, 与上传共用顺序和数量校验. 不隐式覆盖已有图片, 超量时拒绝整批并提示剩余容量, 图片读取期间防止重复输入. 保留普通文字粘贴与显式更换, 在局部重绘和预览弹窗中阻止参考图追加. 不恢复基础画风快捷项, 风格模板继续来自风格库.
- 支持最多 4 张参考图, 每张不超过 12 MiB, 按顺序逐张管理并完整保存到草稿/outbox/配方. 从参考图生成切换局部重绘时固定使用第一张图片, 沿用其原参数, 清除旧蒙版, 不自动打开弹窗. 在局部重绘只展示单张原图卡片和绘制/编辑蒙版入口, 不显示多图槽位或原图选择器; 将其余图片保留在草稿中, 切回参考图生成时恢复完整顺序. 取消蒙版编辑时保留进入弹窗前的草稿, 保存后仅提交该原图和蒙版. 未保存蒙版前阻止生成, 按输出张数计费. 保持单份创作草稿, 不引入双模式独立草稿.
- 在单图提示词下方用紧凑卡片展示模板示例图和名称, 提供更换与移除, 不展示作者, 分类或常驻说明, 不恢复旧名称标签. 在风格库保留作者署名. 保存模板快照, 移除关联保留已编辑文案, 不将示例图自动加入参考图. 在切图工具提供当前完成批次的 ZIP 下载, 保留单张下载, 防止换图或修改参数混用旧批次.
- 保持系列梗概/分镜剧本统一输入, 从中提取主体要求. 不添加独立主体设定字段, 保留可选参考图和首镜参考链.
- 取消系列应用场景预设, 根据输入用途和完整程度规划分镜. 对短梗概适度补充, 对完整剧本保留顺序, 品牌, 引用文字, 占位符和限制, 不强套固定题材. 保存分镜的完整换行, 将输入上限设为 16000 字, 每镜提示词上限设为 5000 字. 拆解阶段只分析文字, 不声称已识别参考图.
- 在系列参考区支持最多 4 张用户参考图, 多选添加, 逐张更换/移除和清空, 每张最多 12 MiB. 与单图参考模式共用粘贴/拖拽追加规则, 已有图片不被覆盖, 超量或读取失败时拒绝整批; 保留普通文字粘贴. 将参考图顺序完整保存到草稿, outbox 和配方. 将首镜或重绘原图作为独立衔接参考追加在用户图片之后, 不截断用户参考图或改变其编号, 按输出张数计费.
- 对单图参考模式与系列复用四个横向槽位, 空位点击添加, 增减图片不改变槽位行高, 窄屏保持四槽同时可见. 用编号识别图片, 点击缩略图查看原图, 分离预览与更换/移除按钮. 复用原有缩放控件, 将弹窗挂载到页面顶层, 关闭后恢复焦点, 预览期间阻止生成快捷键. 在单图局部重绘只预览当前原图, 不展示其他参考图, 不增加锁定强度或保真百分比控件.
- 将系列拆解与生图分开: 拆解只生成可编辑草稿, 经用户检查并点击确认生成后才入队. 不在拆解成功, 草稿恢复或全局快捷键中自动提交图片任务.
- 通过 `/api/styles` 读取已上架风格和真实分类数. 仅在首次启动风格库时导入 `server/style-seed/catalog.json` 的 36 款及对应示例图; 更新和重启不得覆盖编辑或恢复删除项.
- 保持风格录入以图片和完整提示词为必填, 作者可留空, 名称/分类/来源/排序为可选项. 不添加标签, 英文名或独立画风描述. 保留原文, 不自动翻译或追加隐藏画风.
- 让整张风格卡片打开编辑弹窗, 保留独立上下架/删除动作. 在表单直接展示全部录入字段, 不折叠可选信息; 电脑端使用四列素材卡片.
- 将仪表盘的灵感点用量和创作次数分别提供今日/本月/累计切换, 用最近 365 天的真实成功结算记录绘制活跃度热力图. 固定按北京时间的结算日期统计, 保留点值/模式快照与已删除码历史; 风格数量, 访问码状态和待核实数量展示当前值.
- 将风格模板仅用于单图创作. 系列从梗概/分镜的画面要求, 可选参考图和首镜参考链保持连贯, 不增加风格库选择入口.
- 对风格和访问码管理使用独立管理员会话, 不与普通生成权限互相授权. 保存采用版本校验, 风格备份只包含图片与文本, 导入先预览并全量校验后事务合并; 完整服务备份另外保存点数数据库.
- 在接受新增/修改/导入前校验整个风格库可完整导出和恢复, 不生成超过导入限制的备份. 清理无引用示例图, 保留初始素材署名与许可.
- 将本机手动测试与 Docker 的数据目录和网页地址隔离; 不修改真实部署密码和图像通道来运行自动测试.
- 将临时公网隧道放在 Compose 的可选 `tunnel` profile, 通过本机 `.env` 显式启用. 使用独立 `cloudflared` 容器连接芽绘台的容器端口, 不挂载应用配置和数据. 将可选 IPv6 出站放在隧道专用网络, 不改变应用网络; 先确认实际 Docker context 和代理路径再排查连通性. 验证整个 Compose 项目的启动/停止, 不将依赖启动顺序表述为单容器持续联动; 提醒临时地址重启后变化及浏览器存储的地址隔离.
- 使用 `docker-compose.named-tunnel.yml` 接入由 Cloudflare 控制台管理的正式隧道. 从本机 `.env` 的 `TUNNEL_TOKEN` 创建 Compose secret, 通过 `--token-file` 读取, 不将令牌写入命令参数或容器环境变量. 新域名验证通过后才切换正式服务并结束临时入口与验证连接器; 复用应用容器和数据, 不在应用代码里写死域名.
- 通过 Tailwind 3 + PostCSS 编译 React 源码类名, 不依赖设计稿目录参与生产构建. 复用语义色与本地图标字体.
- 先在本机完成相关验证, 再按授权提交推送. 浏览器交互使用同版本 Linux Chromium 容器补充验证; Docker 使用独立原生数据卷验证重启, 重建和文件权限. 将 GitHub CI 作为复核.
- 修改后运行直接相关测试. 发布前检查 `npm test`, 浏览器验收, Docker 验收和 `git diff --check`.
