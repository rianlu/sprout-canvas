services:
  sprout-canvas:
    container_name: sprout-canvas
    build: .
    ports:
      - "8888:8787"
    volumes:
      - ./config:/app/config
      - ./logs:/app/logs
      - ./output:/app/output
    environment:
      TZ: Asia/Shanghai
      NODE_ENV: production
    restart: unless-stopped
